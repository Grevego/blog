# Environment Configuration
# Set to "development" for SQLite or "production" for PostgreSQL
ENVIRONMENT=development

# Database Configuration
# Option 1: Use DATABASE_URL directly (overrides other settings)
# DATABASE_URL=sqlite:///./blog.db
# DATABASE_URL=postgresql://user:password@localhost:5432/blog

# Option 2: Use individual PostgreSQL settings (for production)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=blog

# FastAPI Configuration
APP_NAME=FastAPI Blog
DEBUG=true
API_V1_PREFIX=/api/v1

# Security (for future authentication features)
SECRET_KEY=your-secret-key-here-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

